# Day 1
----
# Day 2
----
# Day 3
----
- Add user to sudo
        `adduser choip`
        `usermod -a -G sudo choip`
    can also be accomplish by modifying the `/etc/sudoers` using the command `visudo`

- change password
        `passwd`


